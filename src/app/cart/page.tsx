import type {Metadata} from 'next';

export const metadata: Metadata = {
  description: 'Корзина',
  title: 'Корзина',
};

export default function Page() {
  return (
    <div>
      cart
    </div>
  );
}
