import type {Metadata} from 'next';

export const metadata: Metadata = {
  description: 'Услуги',
  title: 'Услуги',
};

export default function Page() {
  return (
    <div>
      <h1>Services Page</h1>
    </div>
  );
}
